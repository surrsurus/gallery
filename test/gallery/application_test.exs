defmodule Gallery.ApplicationTest do
  use ExUnit.Case, async: true

  # describe "Application" do
  #   test "can change config" do
  #     assert Gallery.Application.config_change([:foo], [], [:bar]) == :ok
  #   end
  # end
end
